Virtual Voting System
This project is a simple virtual voting system implemented in C. The system allows users to vote for a candidate, counts the votes, and displays the results. Each voter is identified by a unique voter ID to ensure that each person can only vote once.
